# Application has to fetch property source from config-server. 
# The configuration required on the client side is an application name and config server’s connection settings.
spring:
  application:
    name: discovery-service
  cloud:
    # The configuration file discovery-service.yml should be placed inside config-service module. 
    config:
      uri: http://localhost:8088